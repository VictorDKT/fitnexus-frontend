import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    inputContainer: {
        width: "100%",
        marginBottom: 0,
    },

    exerciseContainer: {
        marginBottom: 20,
    },

    footer: {
        flex: 1,
        flexDirection: "row",
        marginBottom: 10,
    },

    buttonBox: {
        width: "50%",
        paddingRight: 10,
    },

    buttonBox2: {
        width: "50%",
        paddingLeft: 10,
    },

    inputLabel: {
        fontSize: 16,
        color: "#fff",
        marginBottom: 5,
    }
});

export default styles;