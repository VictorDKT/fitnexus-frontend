import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    page: {
        padding: 20,
    },

    itemCOntainer: {
        marginBottom: 10,
    },
})

export default styles;